from typing import Optional


def summarize_image_url(image_url: str, summary_type: str = "simple") -> str:
    """Minimal stub for URL-based image summary.
    Replace with real summarization if available.
    """
    return f"Resumen ({summary_type}) de la imagen en {image_url}"


def summarize_image_base64(image_base64: str, summary_type: str = "simple") -> str:
    """Minimal stub for base64-based image summary.
    Replace with real summarization if available.
    """
    return f"Resumen ({summary_type}) de la imagen base64"


