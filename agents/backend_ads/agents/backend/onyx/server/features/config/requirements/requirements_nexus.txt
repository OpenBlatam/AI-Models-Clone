# ============================================================
# NEXUS OPTIMIZER REQUIREMENTS - Ultra-Optimized 2024
# ============================================================

# Core async framework (required)
aiohttp>=3.9.3
asyncio-throttle>=1.0.2
psutil>=5.9.8

# Ultra-fast serialization (Rust-based - HIGHLY RECOMMENDED)
orjson>=3.9.15  # 10x faster JSON
msgpack>=1.0.8  # Binary serialization
simdjson>=6.0.2  # SIMD JSON parsing (optional)

# Ultra-fast hashing & compression (RECOMMENDED)
xxhash>=3.4.1  # Fast non-crypto hash
lz4>=4.3.3  # Ultra-fast compression
zstandard>=0.22.0  # Facebook's Zstd

# High-performance data processing (OPTIONAL)
numpy>=1.24.0  # Numerical arrays
numba>=0.59.0  # JIT compilation
polars>=0.20.7  # 100x faster than pandas (optional)

# Database optimizations (IF USING DATABASES)
asyncpg>=0.29.0  # Async PostgreSQL
aioredis>=2.0.1  # Async Redis
sqlalchemy>=2.0.25  # ORM (optional)

# Event loop optimization (UNIX ONLY)
uvloop>=0.19.0; sys_platform != "win32"  # 4x faster event loop

# Monitoring & logging (RECOMMENDED)
structlog>=23.2.0  # Structured logging
prometheus-client>=0.20.0  # Metrics (optional)

# Additional high-performance libraries (OPTIONAL)
cachetools>=5.3.2  # Caching utilities
memory-profiler>=0.61.0  # Memory profiling
py-spy>=0.3.14  # Performance profiler

# Development & testing
pytest>=8.0.0
pytest-asyncio>=0.23.5
pytest-benchmark>=4.0.0 