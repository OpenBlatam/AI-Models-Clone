---
# Ansible Collections
collections:
  - name: amazon.aws
    version: ">=2.0.0"
  - name: community.general
    version: ">=7.0.0"
  - name: community.docker
    version: ">=3.0.0"

# Ansible Roles (if using external roles)
# roles:
#   - name: geerlingguy.docker
#     version: ">=4.0.0"

